# Railway always prefers a root Dockerfile when present — keep Docker optional under docker/.
[build]
builder = "RAILPACK"

[deploy]
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
